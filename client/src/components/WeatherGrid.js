import WeatherElement from "./WeatherElement";

const WeatherGrid = ({ weather }) => {

    // const weatherNodes = weather.map((weather,index) => {
    //     return <WeatherElement key={index} />
    // })

    return (
        <>
            <h1>Weather Grid</h1>
        
        </>
    )
}

export default WeatherGrid;